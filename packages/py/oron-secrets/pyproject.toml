[project]
name = "oron-secrets"
version = "0.1.0"
description = "GCP Secret Manager environment adapter retained from Or-on"
requires-python = ">=3.14,<3.15"
dependencies = ["google-cloud-secret-manager==2.30.0"]

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"
