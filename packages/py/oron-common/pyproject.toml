[project]
name = "oron-common"
version = "0.1.0"
description = "Shared call context, phone, and usage models retained from Or-on"
requires-python = ">=3.14,<3.15"
dependencies = [
  "phonenumbers==9.0.38",
  "pydantic==2.13.5",
]

[build-system]
requires = ["hatchling==1.32.0"]
build-backend = "hatchling.build"
