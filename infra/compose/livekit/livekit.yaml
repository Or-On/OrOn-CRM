# Local control-plane configuration only. Credentials are injected from the
# ignored developer environment through LIVEKIT_KEYS.
port: 7880
bind_addresses:
  - 0.0.0.0
rtc:
  tcp_port: 7881
  udp_port: 7882
  use_external_ip: false
redis:
  address: redis:6379
logging:
  level: info
  json: true
