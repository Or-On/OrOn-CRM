import { LoadingSkeleton } from "@or-on/ui";

export default function Loading() {
  return (
    <main aria-label="Loading page">
      <LoadingSkeleton label="Loading platform page" />
    </main>
  );
}
